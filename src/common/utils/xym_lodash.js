
export const intersection = (arr1, arr2) => {
    let arr = [];
    arr1.map((item) => {
        if(arr2.includes(item)) {
            arr.includes(item) || arr.push;
        }
        return arr;
    })
}